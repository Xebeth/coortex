interface:
  display_name: "Coortex Review Lane"
  short_description: "Review one bounded Coortex lane"
  default_prompt: "Use $coortex-review-lane to review one bounded Coortex lane. Stay inside the supplied surface, family, or return-review scope. Check the requested contract or closure gate before style or cleanup concerns, apply any configured lenses in order, gather explicit file-backed evidence, and prefer visible uncertainty over rounding up to a pass. If the lane prompt provides an exact output schema, follow it instead of the default findings/verdict format."
