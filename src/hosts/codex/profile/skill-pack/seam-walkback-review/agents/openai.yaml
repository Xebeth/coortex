interface:
  display_name: "Seam Walkback Review"
  short_description: "Archaeology-driven seam cleanup workflow"
  default_prompt: "Use $seam-walkback-review to walk back pivot commits, extract stable seams, refresh review baselines, run targeted seam reviews or bounded slice reviews, repair the owning seam on current HEAD, run a touched-files-only anti-slop cleanup pass, verify, and commit each slice atomically."
