interface:
  display_name: "Seam Walkback Review"
  short_description: "Archaeology-driven seam cleanup workflow"
  default_prompt: "Use $seam-walkback-review to walk back pivot commits, extract stable seams, refresh review baselines, run targeted seam reviews or bounded slice reviews, repair the owning seam on current HEAD, run $coortex-deslop on touched files only, verify, and commit each slice atomically."
